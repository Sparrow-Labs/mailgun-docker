mailgun-docker
==============

Send a mail via mailgun

## Usage

```
docker run \
  -e API_KEY=XXX \
  -e DOMAIN=mydomain.com \
  -e HOST=www01 \
  -e SUBJECT=redis01 started \
  sparrowlabs/mailgun
```
